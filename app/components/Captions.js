import Exponent from 'exponent';
import React from 'react';
import {
  StyleSheet,
  Text,
  View,
  TouchableHighlight
} from 'react-native';

export default Captions = (props) => (
   <View>
   <TouchableHighlight> 
     <Text>English</Text>
   </TouchableHighlight>
   </View>
)